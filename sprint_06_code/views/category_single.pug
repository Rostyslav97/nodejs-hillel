extends template

include mixins/product_card.pug

block content
  .container.mt-4
    h1.text-center.mb-4.text-capitalize= `${category} Catalog`

    .row
      each item in goods
        +productCard(item)